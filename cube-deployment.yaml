apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tcc-postgres-ingress
  namespace: cal-core
spec:
  ingressClassName: nginx
  rules:
  - host: "https://budgetsmart.stk8s.66bit.ru/"
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: tcc-postgres-service
            port:
              number: 5432
