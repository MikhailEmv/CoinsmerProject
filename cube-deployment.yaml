apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tcc-postgres-ingress
  namespace: cal-core
spec:
  ingressClassName: nginx
  rules:
  - host: "hub.66bit.ru/shared/stk8s:1.26.2"
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: tcc-postgres-service
            port:
              number: 5432
